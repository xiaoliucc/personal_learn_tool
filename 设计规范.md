# 学习收集工具 — UI 设计规范

## 推荐工具

| 工具 | 类型 | 说明 |
|------|------|------|
| **Penpot** | 开源/Web | https://penpot.app — 类 Figma，完全开源 |
| **Figma** | 免费/Web | https://figma.com — 行业标准，免费版够用 |
| **Lunacy** | 免费/Windows | 离线设计工具，支持打开 .sketch |

建议用 **Penpot**（开源）或 **Figma**（生态好）。

---

## 配色方案

```
主背景:     #f9fafb (gray-50)
卡片白:     #ffffff
主文字:     #1f2937 (gray-800)
次要文字:   #6b7280 (gray-500)
边框:       #f3f4f6 (gray-100)

侧边栏:     #111827 (gray-900)
侧边栏文字: #d1d5db (gray-300)

主色调:     #3b82f6 (blue-600)     — 按钮、链接、选中态
成功色:     #10b981 (green-600)   — 笔记图标、完成状态
警告色:     #f59e0b (amber-600)   — 链接图标
紫色:       #8b5cf6 (purple-600)  — 代码片段图标

AI 蓝色:    #eff6ff (blue-50)      — AI 进度背景
AI 紫色:    #faf5ff (purple-50)   — 完整处理背景
```

---

## 布局

```
┌──────────┬──────────────────────────────────────────┐
│          │                                          │
│  侧边栏   │              主内容区                     │
│  w-56     │              flex-1                      │
│ (224px)  │                                          │
│          │                                          │
│  可折叠   │                                          │
│  w-16     │                                          │
│ (64px)   │                                          │
└──────────┴──────────────────────────────────────────┘
```

- 全屏高度布局（`h-screen`）
- 侧边栏固定宽度，左侧
- 主内容区可滚动

---

## 页面清单（5 页）

### 1. 仪表盘 `/`
- 顶部标题：「学习仪表盘」
- 5 个统计卡片（一行）：资料总数 / 笔记 / 链接 / 代码片段 / 待复习
- 「最近添加」列表：5 条，每条显示类型图标 + 标题 + 日期

### 2. 资料库 `/library`
- 顶部：「资料库」+「+ 添加资料」按钮（蓝色）
- 筛选栏：搜索框 + 类型下拉
- 资料卡片列表（纵向排列）：
  - 每张卡片：类型图标 + 标题 + 标签 + 日期 + 内容摘要（2行截断）
  - 未处理的有「待 AI 处理」标签
- 底部：分页按钮
- 「添加资料」弹窗：
  - 类型选择（note/link/snippet）
  - 标题、内容（Markdown）、来源输入框
  - 根据类型条件显示 URL（link）或语言（snippet）

### 3. 资料详情 `/materials/:id`
- 顶部：类型标签 + 标题 + 工具栏按钮
  - 🤖 生成摘要 / 🧠 完整处理 / 🔗 发现关联 / 📝 加入复习 / 编辑 / 删除
- AI 进度条（处理中时显示）
- 元数据行：来源、链接、语言、创建时间
- 标签区域（彩色徽章）
- AI 摘要面板（可折叠，含关键要点列表）
- 关联资料列表
- Markdown 正文（表格、代码块、引用）

### 4. 知识图谱 `/graph`
- 力导向图（vis-network）
- 节点 = 资料（box 形状，按类型着色）
- 边 = 关联（标注类型）
- 图例：📝笔记(蓝) / 🔗链接(黄) / 💻代码片段(紫)
- 点击节点跳转详情

### 5. 复习 `/review`
- 进度：当前/总数
- 复习卡片：
  - SM-2 元数据（间隔天数、复习次数）
  - 「显示内容」按钮 → 展开 Markdown
  - 5 级评分按钮：完全忘了(0) / 模糊(2) / 勉强(3) / 较熟悉(4) / 完美(5)

---

## 组件库

| 组件 | 用途 | 出现页面 |
|------|------|----------|
| TagBadge | 彩色标签徽章（含 AI 置信度） | 资料库、详情 |
| MaterialCard | 资料摘要卡片 | 资料库、首页 |
| MaterialForm | 创建/编辑表单（弹窗） | 资料库 |
| SearchBar | 搜索输入（带防抖） | 资料库 |
| MarkdownRenderer | Markdown → HTML 渲染 | 详情、复习 |
| SummaryPanel | AI 摘要面板 | 详情 |
| AIProcessingIndicator | AI 进度条 | 详情 |
| ReviewCard | 复习卡片 | 复习页 |
| AppSidebar | 侧边导航栏 | 全局 |

---

## 交互规范

- 导航：点击侧边栏切换页面（高亮当前页）
- 创建：弹窗模式（Modal），点击遮罩关闭
- AI 操作：点击触发 → 进度条实时更新 → 结果自动刷新
- 删除：确认弹窗
- 编辑：内联切换（查看 ↔ 编辑模式）
- 表格：悬停高亮行
- 卡片：悬停阴影 + 边框变蓝

---

## 开发与设计协作

设计工具中完成 UI 设计后，可以：

1. **直接参考设计稿手写 Tailwind 类**（推荐，因为我们已搭建好组件框架）
2. **导出 SVG/PNG** 作为参考，微调现有组件的间距、颜色、布局
3. **Penpot 支持 CSS 导出**（部分），可辅助生成样式代码
