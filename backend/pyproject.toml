[project]
name = "learning-collector-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "aiosqlite",
    "pydantic",
    "anthropic",
    "openai",
    "python-dotenv",
]
