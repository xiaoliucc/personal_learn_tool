# 开发进度记录

> 最后更新：2026-06-10

---

## 当前状态

**全部七个阶段完成 ✅**

---

## 已完成

### 第一阶段：基础搭建（MVP 核心）✅
- FastAPI + SQLite 后端骨架（异步 SQLAlchemy）
- Material / Tag / MaterialTag 模型（含层级标签）
- 资料 + 标签 CRUD API（分页、筛选、FTS5 搜索）
- Vue 3 + TypeScript + Vite + Tailwind CSS 前端
- Dashboard / Library / MaterialDetail 页面
- markdown-it 渲染（GFM 表格支持）
- .gitignore + README
- 数据分离存储（`~/.learning-collector/`）

### 第二阶段：AI 自动分类与摘要 ✅
- Summary 模型
- ai_service：Anthropic Claude + DeepSeek 双 Provider
- WebSocket 实时进度推送
- `.env` 集中配置（`~/.learning-collector/.env`）
- AIProcessingIndicator + SummaryPanel 组件

### 第三阶段：知识关联与图谱 ✅
- MaterialConnection 模型
- `find_connections` AI 函数
- `/api/graph` + `/api/ai/connect/{id}` 端点
- vis-network 力导向图
- 资料详情页关联侧边栏

### 第四阶段：间隔重复复习 ✅
- ReviewItem 模型（SM-2 算法）
- review_service（SM-2 实现 + 到期查询 + 评分更新）
- ReviewPage（卡片翻页 + 5 级评分 + 自动调度）
- 首页待复习计数 + 详情页「加入复习」按钮

### 第五阶段：打磨完善 ✅
- 暗色模式（CSS 变量 + localStorage 记忆 + 侧边栏切换）
- JSON 全量备份导出
- Markdown 导出（`.zip`，含 frontmatter，可导入 Obsidian）
- 长链接截断优化
- 基于 Open Design 设计稿的全局样式重构

### 第六阶段：PWA 离线支持 ✅
- vite-plugin-pwa：Service Worker 自动缓存
- Web Manifest：可安装到桌面
- API 运行时缓存（NetworkFirst 策略）
- 自动更新

### 第七阶段：浏览器扩展 ✅
- Chrome/Edge Manifest V3 扩展
- 一键保存当前网页链接 + 标题
- 支持标签（逗号分隔）+ 备注
- 记忆上次标签
- `Ctrl+Enter` 快捷键

### Bug 修复
- Edge 浏览器最小化按钮失效：vue-router 5.x → 4.5.1 降级修复
- vis-network tooltip 布局崩溃：改用 box 形状 + 固定高度
- WebSocket 离开页面后无限重连：添加 `intentionalClose` 标志
- 侧边栏消失：回滚时漏了 Pinia 安装
- Markdown 表格无法渲染：替换为 markdown-it

---

## 最终项目结构

```
personal/
├── backend/
│   └── app/
│       ├── models/       Material, Tag, MaterialTag, Summary, MaterialConnection, ReviewItem
│       ├── schemas/      material.py, tag.py
│       ├── routes/       materials, tags, ai, graph, review, export
│       ├── services/     ai_service (Claude+DeepSeek), review_service (SM-2)
│       ├── ws_manager.py
│       ├── main.py
│       ├── config.py     (含 .env 加载)
│       └── database.py
├── frontend/
│   └── src/
│       ├── style.css     设计令牌 + 暗色模式
│       ├── api/          client, materials, tags, ai, graph, review
│       ├── composables/  useWebSocket
│       ├── stores/       materialStore, uiStore (Pinia)
│       ├── router/       vue-router@4.5.1 + createWebHashHistory
│       ├── components/   layout, materials, tags, ai, graph, review, shared
│       ├── pages/        Dashboard, Library, MaterialDetail, KnowledgeGraph, Review
│       └── types/
├── browser-extension/    manifest.json, popup.html, popup.js
├── .gitignore
├── README.md
├── 实现计划.md
├── 设计规范.md
├── 开发进度.md
└── Edge浏览器最小化Bug.md
```

---

## 依赖版本

| 包 | 版本 | 备注 |
|----|------|------|
| vue | 3.x | |
| vue-router | **4.5.1** | 5.x 与 Edge 兼容 bug |
| pinia | 2.x | |
| tailwindcss | 4.x | @tailwindcss/vite |
| markdown-it | latest | GFM Markdown |
| vis-network | latest | 知识图谱 |
| vite-plugin-pwa | latest | PWA |
| fastapi | latest | |
| sqlalchemy | latest | 异步 |
| anthropic | latest | Claude API |
| openai | latest | DeepSeek API |

---

## 启动命令

```bash
# 后端（先配置 ~/.learning-collector/.env）
uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000

# 前端
cd frontend && npm run dev
```
