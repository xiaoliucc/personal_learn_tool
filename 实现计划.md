# 个人学习收集工具 - 实现计划

## 背景与目标

构建一个**基于 Web 的个人学习收集工具**，帮助用户收集、整理和复习学习资料。通过 AI Agent（Claude API）自动对资料进行分类、摘要、知识关联，并生成复习计划。通过 WebSocket 实现实时进度推送，让用户在 AI 处理过程中获得即时反馈。

**技术栈**：Python FastAPI + SQLite（后端）| Vue 3 + TypeScript + Vite + Tailwind CSS（前端）| Anthropic Claude API（AI 驱动）

---

## 用户数据与代码分离方案

### 设计原则
项目代码上传到 GitHub，用户的学习资料（数据库、配置中的 API Key 等）**不进入版本控制**，存储在独立目录中。

### 数据存储位置
- **默认数据目录**：`~/.learning-collector/`（用户主目录下）
  - `data.db` — SQLite 数据库（学习资料、标签、摘要等全部用户数据）
  - `.env` — API Key 等敏感配置（可选）
- **可通过环境变量覆盖**：`LEARNING_DATA_DIR=/path/to/data`
- **后端启动时**：自动检查数据目录是否存在，不存在则创建并初始化空数据库

### .gitignore 配置
```gitignore
# 用户数据目录（整个项目不包含任何 .db 文件）
*.db
*.db-journal
*.db-wal

# 环境配置（含 API Key）
.env
.env.local

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
dist/

# IDE
.vscode/
.idea/
```

### GitHub 上传清单
| 上传 | 不上传 |
|------|--------|
| 所有项目源代码 | `*.db` 数据库文件 |
| README.md | `.env` 配置文件 |
| requirements.txt / package.json | `node_modules/` / `.venv/` |
| .gitignore | 任何用户学习资料内容 |

---

## 项目结构

```
personal/
├── backend/
│   ├── app/
│   │   ├── main.py              # FastAPI 入口，WebSocket 配置
│   │   ├── config.py            # 配置（数据目录、API Key 等）
│   │   ├── database.py          # SQLAlchemy 引擎和会话（数据库路径指向数据目录）
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   ├── material.py      # Material, Tag, MaterialTag
│   │   │   ├── summary.py       # Summary（AI 摘要）
│   │   │   ├── connection.py    # MaterialConnection（知识关联）
│   │   │   └── review.py        # ReviewPlan
│   │   ├── schemas/             # Pydantic 请求/响应模型
│   │   │   ├── material.py
│   │   │   ├── tag.py
│   │   │   └── ai.py
│   │   ├── routes/
│   │   │   ├── materials.py     # 资料 CRUD + AI 处理触发
│   │   │   ├── tags.py          # 标签 CRUD
│   │   │   ├── ai.py            # AI 相关接口（分类、摘要、关联、复习计划）
│   │   │   ├── search.py        # 全文搜索
│   │   │   └── graph.py         # 知识图谱数据
│   │   ├── services/
│   │   │   ├── ai_service.py    # Claude API 调用，prompt 模板
│   │   │   ├── tag_service.py   # 标签管理逻辑
│   │   │   └── review_service.py# 间隔重复算法（SM-2）
│   │   └── ws_manager.py        # WebSocket 连接管理
│   ├── requirements.txt
│   └── alembic/                 # 数据库迁移（可选）
├── frontend/
│   ├── src/
│   │   ├── main.ts              # Vue 应用入口
│   │   ├── App.vue              # 根组件
│   │   ├── api/                 # API 调用封装
│   │   │   ├── client.ts        # fetch/axios 封装
│   │   │   ├── materials.ts
│   │   │   ├── tags.ts
│   │   │   └── ai.ts
│   │   ├── composables/         # Vue 组合式函数（替代 React hooks）
│   │   │   ├── useWebSocket.ts  # WebSocket 连接
│   │   │   ├── useMaterials.ts
│   │   │   └── useAITask.ts     # AI 任务状态追踪
│   │   ├── stores/              # Pinia 状态管理
│   │   │   ├── materialStore.ts
│   │   │   └── uiStore.ts
│   │   ├── components/
│   │   │   ├── layout/
│   │   │   │   ├── AppSidebar.vue       # 侧边栏导航
│   │   │   │   └── AppLayout.vue        # 主布局
│   │   │   ├── materials/
│   │   │   │   ├── MaterialCard.vue     # 资料卡片
│   │   │   │   ├── MaterialForm.vue     # 资料创建/编辑表单
│   │   │   │   └── MaterialList.vue     # 资料列表（含筛选）
│   │   │   ├── tags/
│   │   │   │   └── TagBadge.vue         # 标签徽章
│   │   │   ├── ai/
│   │   │   │   ├── AIProcessingIndicator.vue  # AI 处理进度
│   │   │   │   └── SummaryPanel.vue     # AI 摘要面板
│   │   │   ├── graph/
│   │   │   │   └── KnowledgeGraph.vue   # 知识图谱可视化
│   │   │   ├── review/
│   │   │   │   └── ReviewCard.vue       # 复习卡片
│   │   │   └── shared/
│   │   │       ├── MarkdownRenderer.vue # Markdown 渲染
│   │   │       ├── CodeBlock.vue        # 代码高亮
│   │   │       └── SearchBar.vue        # 搜索栏
│   │   ├── pages/
│   │   │   ├── DashboardPage.vue        # 首页仪表盘
│   │   │   ├── LibraryPage.vue          # 资料库
│   │   │   ├── MaterialDetailPage.vue   # 资料详情
│   │   │   ├── KnowledgeGraphPage.vue   # 知识图谱页
│   │   │   ├── ReviewPage.vue           # 复习页
│   │   └── types/
│   │       └── index.ts                 # TypeScript 类型定义
│   ├── index.html
│   ├── package.json
│   ├── tsconfig.json
│   ├── vite.config.ts
│   └── tailwind.config.js
├── browser-extension/            # Chrome/Edge 浏览器扩展
│   ├── manifest.json
│   ├── popup.html
│   ├── popup.js
│   └── icons/
├── .gitignore                   # 排除数据文件、依赖、敏感配置
└── README.md
```

### 前端技术对应关系（React → Vue 3）

| 概念 | React | Vue 3 |
|------|-------|-------|
| 组件文件 | `.tsx` | `.vue`（SFC 单文件组件） |
| 组合式逻辑 | Custom Hooks | Composables（`useXxx`） |
| 状态管理 | Zustand | Pinia |
| 路由 | react-router-dom | vue-router |
| 响应式 | useState/useEffect | ref/reactive/watch |
| 模板语法 | JSX | `<template>` + 指令 |
| 样式方案 | Tailwind CSS | Tailwind CSS（不变） |

---

## 数据模型

### Material（学习资料）
| 字段        | 类型     | 说明                          |
|-------------|----------|-------------------------------|
| id          | UUID     | 主键                          |
| type        | 枚举     | `note`（笔记）/ `link`（链接）/ `snippet`（代码片段） |
| title       | 字符串   | 必填                          |
| content     | 文本     | 正文（链接类型可为空）         |
| url         | 字符串   | 链接类型的 URL                |
| language    | 字符串   | 代码语言（如 "python"）       |
| source      | 字符串   | 来源（可选）                  |
| is_processed| 布尔     | 是否已被 AI 处理              |
| created_at  | 日期时间 |                               |
| updated_at  | 日期时间 |                               |

### Tag（标签）
| 字段        | 类型     | 说明                              |
|-------------|----------|-----------------------------------|
| id          | UUID     | 主键                              |
| name        | 字符串   | 唯一                              |
| color       | 字符串   | 十六进制颜色                       |
| parent_id   | UUID     | 父标签 ID（自引用外键，可选）       |
| description | 字符串   | 可选                              |

> **标签层级**：支持两级标签结构。如 `Python`（父）→ `asyncio`（子）。前端展示为层级标签树，筛选时可选择父标签自动包含子标签。

### MaterialTag（资料-标签关联表）
material_id (FK) + tag_id (FK) + confidence 置信度（Float，AI 建议的标签带置信度）

### Summary（AI 摘要）
| 字段        | 类型     | 说明                            |
|-------------|----------|---------------------------------|
| id          | UUID     | 主键                            |
| material_id | UUID     | 外键，唯一（1:1）               |
| content     | 文本     | 摘要内容                        |
| key_points  | JSON     | 关键要点列表（字符串数组）       |
| generated_at| 日期时间 |                                 |
| model_used  | 字符串   | 如 "claude-sonnet-4-6"          |

### MaterialConnection（知识关联）
| 字段          | 类型   | 说明                                          |
|---------------|--------|-----------------------------------------------|
| id            | UUID   | 主键                                          |
| material1_id  | UUID   | 外键                                          |
| material2_id  | UUID   | 外键                                          |
| relation_type | 字符串 | "prerequisite"（前置）/ "extends"（扩展）/ "related"（相关）/ "contradicts"（矛盾） |
| description   | 文本   | 关联说明                                      |
| strength      | Float  | 关联强度 0.0 - 1.0                            |
| generated_at  | 日期时间 |                                             |

### ReviewPlan（复习计划）
| 字段         | 类型     | 说明                                   |
|--------------|----------|----------------------------------------|
| id           | UUID     | 主键                                   |
| name         | 字符串   | 计划名称                               |
| material_ids | JSON     | 待复习的资料 UUID 列表                  |
| schedule     | JSON     | 间隔重复时间表                          |
| status       | 枚举     | `active` / `completed` / `archived`    |
| created_at   | 日期时间 |                                        |

---

## API 接口设计

### 资料相关
| 方法   | 路径                          | 说明                                |
|--------|-------------------------------|-------------------------------------|
| GET    | /api/materials                | 列表（分页，可按 type/tags 筛选）    |
| POST   | /api/materials                | 创建新资料                          |
| GET    | /api/materials/{id}           | 获取详情（含标签、摘要、关联）       |
| PUT    | /api/materials/{id}           | 更新资料                            |
| DELETE | /api/materials/{id}           | 删除资料                            |
| POST   | /api/materials/{id}/process   | 触发 AI 处理（分类 + 摘要 + 关联）  |

### 标签相关
| 方法   | 路径              | 说明       |
|--------|-------------------|------------|
| GET    | /api/tags         | 获取全部   |
| POST   | /api/tags         | 创建标签   |
| PUT    | /api/tags/{id}    | 更新标签   |
| DELETE | /api/tags/{id}    | 删除标签   |

### AI 操作
| 方法   | 路径                     | 说明                           |
|--------|--------------------------|--------------------------------|
| POST   | /api/ai/classify/{id}    | 自动打标签                     |
| POST   | /api/ai/summarize/{id}   | 生成摘要和关键要点              |
| POST   | /api/ai/connect/{id}     | 发现与其他资料的关联            |
| POST   | /api/ai/review-plan      | 生成间隔重复复习计划            |

### 搜索与图谱
| 方法   | 路径          | 说明                                  |
|--------|---------------|---------------------------------------|
| GET    | /api/search   | 全文搜索（参数：?q=&type=&tags=）     |
| GET    | /api/graph    | 知识图谱数据（节点 + 边）              |

### WebSocket
| 路径           | 说明                                                       |
|----------------|------------------------------------------------------------|
| /ws/processing | AI 任务实时进度。客户端连接后接收 JSON：`{task_id, status, progress, message}` |

---

## AI 服务设计（Claude API）

### Prompt 策略
每个 AI 任务使用专用的 system prompt + user prompt：

1. **分类/打标签**：传入资料内容 → Claude 返回 `{tags: [{name, confidence}], suggested_colors: [...]}`
2. **生成摘要**：传入资料内容 → Claude 返回 `{summary: "...", key_points: ["..."]}`；对于 `link` 类型，后端可先抓取网页内容再交由 Claude 摘要
3. **发现关联**：传入新资料 + 已有资料的标题/摘要列表 → Claude 返回 `{connections: [{material_id, relation_type, description, strength}]}`
4. **复习计划**：传入资料列表 → Claude 返回 `{plan_name, schedule: [{material_id, review_interval_days, reason}]}`

### 处理流程
- 所有 AI 任务使用 FastAPI 后台任务异步执行
- 立即返回 `task_id`，通过 WebSocket 推送进度
- 多数任务使用 `claude-sonnet-4-6`（速度与成本平衡）
- 内存级并发限制：最多 5 个并发 AI 调用

### 错误处理
- API 错误时最多重试 2 次（指数退避）
- 优雅降级：打标签失败时资料仍保存，`is_processed=false`
- 前端展示错误状态并提供重试按钮

---

## 前端设计（Vue 3）

### 状态管理：Pinia
- `useMaterialStore`：资料列表、筛选条件、当前选中资料
- `useUiStore`：侧边栏状态、弹窗、AI 任务进度
- Pinia 是 Vue 官方推荐的状态管理库，TypeScript 支持完善，API 简洁

### 页面详情

**DashboardPage（首页 `/`）**
- 统计卡片：资料总数、按类型分布、热门标签
- 最近添加的资料（7 天内）
- 快速添加按钮（悬浮操作按钮）
- 待 AI 处理的资料列表
- 今日待复习数量

**LibraryPage（资料库 `/library`）**
- 筛选栏：类型（笔记/链接/代码片段）、标签、搜索关键词
- 卡片/列表视图切换
- 每张卡片展示：标题、类型图标、标签、日期、摘要预览
- 批量操作：打标签、删除、AI 处理

**MaterialDetailPage（资料详情 `/materials/:id`）**
- 完整内容支持 Markdown 渲染 + 代码高亮
- AI 操作工具栏：[生成摘要] [发现关联] [加入复习计划]
- 摘要面板（可折叠）
- 标签区域（手动标签 + AI 建议标签，可接受/拒绝）
- 侧边栏：关联的其他资料
- 编辑模式切换

**KnowledgeGraphPage（知识图谱 `/graph`）**
- 使用 `vis-network` 或 `@vue-flow/core` 绘制力导向图
- 节点 = 资料（颜色按类型区分），边 = 关联（标注关系类型）
- 点击节点 → 跳转资料详情
- 可按标签、时间范围、关联强度筛选

**ReviewPage（复习 `/review`）**
- 间隔重复卡片界面
- 卡片展示：资料标题、摘要/关键要点、"显示完整内容"按钮
- 操作按钮：[记得] [忘了] → 动态调整复习间隔
- 当前复习会话进度条

### 通用组件
- `MaterialCard` — 用于资料库、首页、知识图谱 tooltip
- `TagBadge` — 所有展示标签的地方
- `MarkdownRenderer` — 资料详情、复习卡片
- `AIProcessingIndicator` — 资料详情、资料库（批量操作）
- `CodeBlock` — 代码高亮显示（使用 `highlight.js`）

### Vue 3 组件结构示例
```vue
<!-- 典型 Vue SFC 组件结构 -->
<script setup lang="ts">
import { ref, computed } from 'vue'
import { useMaterialStore } from '@/stores/materialStore'

const store = useMaterialStore()
const isLoading = ref(false)

// 组合式 API 逻辑
</script>

<template>
  <!-- 模板 -->
</template>

<style scoped>
/* 组件样式（Tailwind 为主，scoped 样式为辅） */
</style>
```

---

## 实时推送架构

### WebSocket 流程
1. 客户端连接 `/ws/processing`
2. 客户端通过 HTTP POST 发起 AI 任务 → 获得 `task_id`
3. 后端后台处理，通过 WebSocket 推送进度：
   ```json
   {"task_id": "abc", "status": "running", "progress": 0.5, "message": "正在分析内容..."}
   {"task_id": "abc", "status": "completed", "progress": 1.0, "result": {...}}
   {"task_id": "abc", "status": "failed", "progress": 0.5, "error": "API 超时"}
   ```
4. 前端 `useAITask` composable 管理任务生命周期，响应式更新 UI

### 使用场景划分
- **WebSocket**：AI 处理进度、实时状态更新
- **HTTP**：所有 CRUD、搜索、图谱数据获取
- 无需轮询 —— WebSocket 完全替代

---

## 实施阶段

### 第一阶段：基础搭建（MVP 核心）
**目标**：实现手动模式下的资料 CRUD

**后端**：
- 项目脚手架搭建，FastAPI 应用初始化，SQLite 数据库连接（数据库文件存入 `~/.learning-collector/`）
- 实现数据目录自动创建逻辑（`config.py`）
- Material + Tag 模型、Pydantic Schema、CRUD 路由
- 搜索接口（使用 SQLite FTS5 全文搜索，支持中文分词）

**前端**：
- Vite + Vue 3 + TypeScript + Tailwind CSS 项目初始化
- Vue Router 路由配置
- 侧边栏导航布局（AppLayout + AppSidebar）
- 资料库页面：MaterialList + MaterialCard
- 资料详情页（查看 + 编辑）
- MaterialForm（创建/编辑资料页面/弹窗）
- 基础的标签管理
- Pinia store 搭建

**基础设施**：
- .gitignore 配置（排除数据库、依赖、敏感配置）
- README.md 编写（项目说明 + 启动指南）

### 第二阶段：AI 自动分类与摘要
**目标**：AI 能自动对资料进行分类和生成摘要

**后端**：
- Anthropic SDK 集成到 `ai_service.py`
- `/api/ai/classify` 和 `/api/ai/summarize` 接口
- `/api/materials/{id}/process` 编排接口（分类 + 摘要）
- WebSocket 管理器，广播处理进度

**前端**：
- AIProcessingIndicator 进度组件
- MaterialDetailPage 中集成 SummaryPanel
- 资料库和详情页的「AI 处理」按钮
- useWebSocket composable，实时展示进度
- AI 建议标签 UI（接受/拒绝交互）

### 第三阶段：知识关联
**目标**：自动发现资料间关联，可视化知识图谱

**后端**：
- MaterialConnection 模型
- `/api/ai/connect` 接口
- `/api/graph` 接口（返回节点和边数据）
- 新建资料时可选自动关联

**前端**：
- KnowledgeGraphPage（`vis-network` 或 `@vue-flow/core` 力导向图）
- MaterialDetailPage 侧边栏展示关联资料
- 关联强度指示器

### 第四阶段：复习计划
**目标**：基于收集的资料生成 AI 驱动的复习计划

**后端**：
- ReviewPlan 模型
- `/api/ai/review-plan` 接口
- 间隔重复算法（SM-2 简化版）
- 复习记录和历史

**前端**：
- ReviewPage：间隔重复卡片界面
- 首页集成：今日复习数量

### 第五阶段：打磨完善
- Markdown 编辑器（后续可升级为富文本）
- 暗色模式
- **数据导出**：
  - JSON 格式（完整数据备份，可指定路径，支持重新导入还原）
  - **Markdown 格式**：每篇资料导出为一个 `.md` 文件（含 frontmatter 元数据：标题、标签、日期、摘要），可直接导入 Obsidian、Notion 等笔记工具，避免数据锁定
- 性能优化（分页加载、懒加载）
- 错误边界和加载状态完善

### 第六阶段：PWA 离线支持
**目标**：让 Web 应用可安装，支持离线访问

- 使用 `vite-plugin-pwa` 添加 Service Worker
- 离线缓存：已加载的资料、页面资源可离线查看
- 安装提示：浏览器地址栏显示「添加到桌面」
- manifest.json 配置（应用名称、图标、主题色）

### 第七阶段：浏览器扩展
**目标**：一键收集网页链接，无需打开 Web 应用

- 开发一个简易 Chrome/Edge 扩展（Manifest V3）
- 点击扩展图标 → 弹出小窗口，自动填入当前页面的标题和 URL
- 一键发送到后端保存为 `link` 类型资料
- 可选：自动触发 AI 抓取网页内容并生成摘要
- 扩展目录放在 `browser-extension/` 下

---

## 依赖库

### 后端（requirements.txt）
```
fastapi
uvicorn[standard]
sqlalchemy
aiosqlite
pydantic
anthropic
python-dotenv
```

### 前端（package.json 核心依赖）
```
vue
vue-router
pinia
tailwindcss, postcss, autoprefixer
@vue-flow/core（知识图谱）
highlight.js（代码高亮）
markdown-it（Markdown 渲染）
lucide-vue-next（图标库）
vite-plugin-pwa（PWA 离线支持）
```

---

## 验证方案

1. **第一阶段**：创建各类型资料（笔记/链接/代码片段），增删标签，关键词搜索 —— 确认 CRUD 正常；确认数据存储在 `~/.learning-collector/` 而非项目目录
2. **第二阶段**：点击「AI 处理」→ 验证摘要实时出现、WebSocket 进度推送、标签自动建议
3. **第三阶段**：添加 5+ 条相关资料，运行「发现关联」→ 验证图谱正确渲染、节点可点击跳转
4. **第四阶段**：创建复习计划 → 验证卡片按间隔出现，[记得]/[忘了] 操作调整间隔生效
5. **第五阶段**：切换暗色模式，导出 JSON 和 Markdown → 验证 JSON 可重新导入还原数据，Markdown 文件可在 Obsidian 中打开
6. **第六阶段**：在 Chrome 中打开应用 → 验证地址栏出现安装按钮，安装后离线可打开
7. **第七阶段**：安装浏览器扩展 → 在任意网页点击扩展图标，一键保存链接到资料库
8. **GitHub 检查**：`git status` 确认无 `.db`、`.env` 文件被追踪
